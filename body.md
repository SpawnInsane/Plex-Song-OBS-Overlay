# Release notes

- Added in-app updates: the control window now checks GitHub for a newer version, verifies the download against its published SHA-256 checksum, and can install it and restart on request. An optional setting also offers pre-release (RC) builds.
